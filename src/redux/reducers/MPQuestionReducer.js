
const INITIAL_STATE = {
    questions: [],
    answers: [],
    quizInProgress: false,
  };

export const SPQuestionReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    };
};

