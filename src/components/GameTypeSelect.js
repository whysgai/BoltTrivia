const GameTypeSelect = () => {
    return (
        <>
            <p>Player 1: select a game mode:</p>
            <button className="btn btn-info">Timed</button>
            <button className="btn btn-warning">Score</button>
        </>
    );
};

export default GameTypeSelect;